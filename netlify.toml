# Netlify build + functions config for the SolvyX landing (Astro static).
[build]
  command = "pnpm build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"
